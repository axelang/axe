use std.io;

model Cat {
    annoyingness_level: i32,
    health            : i32,
    name              : ref char
}

def main() {
    mut cat = new Cat(
        health: 100,
        name: "Garfield",
        annoyingness_level: 9999
    );
    cat.health = 90;
    println cat.health;
}

use std.io (
    println_str
);

opaque { MyCustomType, AnotherOpaqueType };

extern def fopen(filename: ref char, mode: ref char): usize;
extern def fclose(file: usize): i32;
extern def fprintf(file: usize, format: ref char): i32;

def main() {
    println_str(string.create("Testing opaque and extern features"));
    println_str(string.create("Compilation successful!"));
}
